# Contributing

Use the standard package commands before sending changes:

```bash
composer fix
composer test
composer test-bc
```

Run `composer test` and `composer test-bc` sequentially because both commands rewrite the dependency set and `vendor/`.

[Report issues](https://github.com/softspring/cms-data-plugin/issues) and [send Pull Requests](https://github.com/softspring/cms-data-plugin/pulls)
